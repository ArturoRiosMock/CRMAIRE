/** Cantidad de tarjetas a mostrar por columna antes de "Ver más" */
export const INITIAL_VISIBLE_PER_COLUMN = 60;

/** Cuántas tarjetas adicionales cargar al hacer clic en "Ver más" */
export const LOAD_MORE_INCREMENT = 60;

/** Máximo a mostrar cuando hay filtro activo (evita colapso con muchos resultados) */
export const MAX_VISIBLE_WHEN_FILTERED = 150;
